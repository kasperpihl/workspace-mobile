import { styleSheet } from 'swiss-react';

export default styleSheet('InitLoading', {
  Wrapper: {
    _flex: ['column', 'flex-start'],
    _size: ['100%', '100%'],
  },
  LoaderContainer: {
    _flex: 'center',
    _size: ['100%', '100%'],
  },
});
