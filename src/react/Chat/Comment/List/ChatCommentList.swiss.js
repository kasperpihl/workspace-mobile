import { styleSheet } from 'swiss-react';

export default styleSheet('ChatCommentList', {
  Wrapper: {
    flex: 1,
    _size: ['100%', '100%'],
  },
  LoaderContainer: {
    _flex: 'center',
    _size: ['100%', '100%'],
  },
});
